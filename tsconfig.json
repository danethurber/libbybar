{
  // Main process (+ shared). Node/Electron runtime — no DOM types.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/main", "src/shared"]
}
